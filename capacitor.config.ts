import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ExamenAppMovil7',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
